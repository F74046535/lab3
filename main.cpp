#include<iostream>
#include<vector>
using namespace_ std;
int main()
{     int number;
    int  weight_in[8];	
	int number element=0;
     ifstream inflie("file2.in",ios::in);
	 while(infile>>number>>weight_in[num_element++]){
		 sort(weight_in);
	 cout<<The five top total weight is:<<count(weight_in)<<endl;}
	 return 0;
	}
